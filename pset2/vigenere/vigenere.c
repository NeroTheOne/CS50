#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[]) {
    printf("argc: %i\n", argc);

    while (argc != 2) {
        printf("./vigenere key[string]");
        return 1;
    }

    for (int j = 0; j < strlen(argv[1]); j++) {

    }
}